#include <stdio.h>
#include <stdlib.h>

#define BUFF_SIZE 4096
char buff[BUFF_SIZE];

int main(){
    int n;
    char digits[7];
    int number;
    int i;
    while ((n = read(1, &buff, sizeof(buff))) > 0)
    {
        for (i = 0; i < 7 ; i++)
        {
            /* code */
        }
        
    }
    
}